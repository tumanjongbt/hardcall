{
  "name": "events",
  "version": "0.1.0",
  "private": true,
  "description": "CLI to push career market events to the ingest API",
  "bin": {
    "events": "./src/events.js"
  },
  "main": "./src/events.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/*.test.js"
  }
}
