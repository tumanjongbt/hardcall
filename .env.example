# Neon Postgres connection string (use sslmode=require).
# For migrations, prefer the direct (non-pooled) host if you use Neon pooled connections.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require

# Listen address (defaults shown).
PORT=3000
HOST=0.0.0.0
